# Prompt สำหรับย้ายหน้าแพทย์ไปใช้ MUI

โปรดปรับ presentation layer ของโมดูลแพทย์ใน `react-frontend/src/pages/doctor` ให้เป็นไปตาม Tech Stack ที่กำหนด โดยยังไม่เปลี่ยนพฤติกรรมของระบบ ข้อมูล หรือ API

## Tech Stack ที่อนุญาต

- React 19
- Vite 8
- TypeScript 6
- MUI Core 6 และ Emotion
- หากจำเป็นต้องใช้ไอคอน ให้ใช้ `@mui/icons-material` รุ่นที่เข้ากันได้กับ MUI 6

ก่อนติดตั้งแพ็กเกจหรือเปลี่ยนเวอร์ชันใด ๆ ให้ตรวจ `package.json` และการใช้งานจริงก่อน ห้ามเปลี่ยน dependency ที่ไม่เกี่ยวข้อง หาก Tailwind CSS หรือ `lucide-react` ยังถูกใช้ในโมดูลอื่น ห้ามลบออกจากทั้งโปรเจกต์

## เป้าหมาย

ย้าย UI ทั้งหมดใน `react-frontend/src/pages/doctor` รวมถึงหน้าหลักและ shared components ไปใช้ MUI (`ThemeProvider`, `createTheme`, `sx` หรือ `styled`) ให้เป็นระบบเดียวกัน รองรับ responsive และคงหน้าตากับ UX ล่าสุดไว้ให้ใกล้เคียงเดิมที่สุด หลังย้ายเสร็จแล้วจึงนำ Tailwind utility classes และการ import `lucide-react` ออกจากโมดูลแพทย์

ห้ามแก้ business logic, hooks, services/API, data types, translations, การเปลี่ยนสถานะผู้ป่วย, validation, การคัดลอกข้อมูล และผลลัพธ์การพิมพ์

## ขอบเขตงาน

ตรวจและย้ายไฟล์ `.tsx` ทั้งหมดใต้ `react-frontend/src/pages/doctor` โดยครอบคลุมอย่างน้อย:

- หน้าคิวผู้ป่วย
- หน้าประวัติเวชระเบียนและรายละเอียดผู้ป่วย
- หน้าบันทึกการตรวจทุกขั้นตอน
- หน้าตารางเวร รายงาน และ loading/empty states
- shared components เช่น stat cards, badges, filter tabs, copyable text, tables, dialogs และ form controls

สร้าง theme และ shared primitives ก่อน เพื่อให้สี ระยะห่าง typography, border radius, shadow, hover, focus และ disabled state สม่ำเสมอ หลีกเลี่ยงการเขียน style ซ้ำหลายหน้า

## UX ที่ต้องรักษาไว้

- การ์ดสถิติหน้าคิวเป็นพื้นขาว การ์ดที่เลือกใช้กรอบสีฟ้า การ hover แสดงเพียงเงาที่กรอบ และ keyboard focus ต้องไม่ทำให้กรอบค้างผิดการ์ด
- ตัวกรองระดับความเร่งด่วนมีความกว้างเท่ากันทุกตัวเลือก และช่องค้นหาในรายการคิวมีขนาดกะทัดรัดตาม UI ปัจจุบัน
- จำนวนประวัติรักษาของผู้ป่วยต้องนับจากรายการจริง ไม่ใช่ค่า boolean 0/1
- ปุ่มกลับหน้าคิวในหน้าบันทึกการตรวจและหน้าประวัติมีพฤติกรรมและ hover เหมือนกัน และยังมองเห็นเมื่อเลื่อนหน้า
- แท็บ 5 ขั้นตอนในหน้าบันทึกการตรวจรองรับปุ่มลูกศรซ้าย/ขวาโดยเรียงตามลำดับ ไม่มีกรอบ UI เพิ่มจาก keyboard navigation และเมื่อเปลี่ยนขั้นตอนต้องเลื่อนไปยังตำแหน่งด้านบนที่ไม่ถูก sticky header บัง
- ด้านบนของหน้าบันทึกการตรวจมีปุ่ม “ยกเลิกการรับบริการ”, “บันทึกฉบับร่าง” และ “บันทึกผลการตรวจ” ส่วนปุ่มซ้ำและหัวข้อสรุปการตรวจด้านล่างถูกนำออกแล้ว
- การ์ดผู้ป่วยในหน้าบันทึกการตรวจคงรูปแบบข้อมูลพื้นฐานล่าสุด ไม่มีจุดคั่นระหว่างคิว, HN, VN และเลขบัตร แสดงระดับความรุนแรง และใช้อวตารขนาดที่ปรับแล้ว
- รายการโรคที่วินิจฉัยเริ่มต้นเป็นค่าว่าง พร้อมข้อความ `ยังไม่มีรายการโรคที่วินิจฉัย จำเป็นต้องระบุการวินิจฉัยหลักอย่างน้อย 1 รายการ`
- คง layout ล่าสุดของข้อมูลคัดกรองและประวัติ, การวินิจฉัยโรค, การสั่งยา, เอกสารและการส่งต่อ และนัดหมายติดตามอาการ
- หน้าประวัติเวชระเบียนคง layout รายละเอียดผู้ป่วยและประวัติแต่ละครั้งล่าสุด ไม่มีจุดคั่นในแถวข้อมูลผู้ป่วย ไม่มีแถบสีน้ำเงินหน้าหัวข้อ และไม่มี HN หรือป้ายจำนวนประวัติรวมในแถวแท็บ
- แท็บ “ประวัติการตรวจรักษาย้อนหลัง” และ “ข้อมูลสุขภาพและประวัติส่วนตัว” กว้างครึ่งบรรทัดเท่ากัน และรองรับปุ่มลูกศรซ้าย/ขวาแบบเดียวกับหน้าบันทึกการตรวจ

## ลำดับการทำงาน

1. สำรวจไฟล์ การพึ่งพา shared components และพฤติกรรมทุกหน้าก่อนแก้
2. สร้าง MUI theme และ shared primitives
3. ย้ายหน้าคิวผู้ป่วย
4. ย้ายหน้าประวัติเวชระเบียน
5. ย้ายหน้าบันทึกการตรวจทุกขั้นตอน
6. ย้ายหน้าตารางเวร รายงาน loading และ empty states
7. ตรวจและลบ Tailwind classes กับ `lucide-react` imports ที่เหลือเฉพาะในโมดูลแพทย์
8. ทดสอบ build, lint, responsive, mouse และ keyboard navigation

แบ่งการเปลี่ยนแปลงเป็นช่วงที่ตรวจสอบได้ หลังจบแต่ละหน้าต้องเช็กว่า UI และการทำงานเดิมยังครบก่อนย้ายหน้าถัดไป ห้ามแก้ไฟล์นอกขอบเขตโดยไม่มีเหตุจำเป็น

## เกณฑ์รับงาน

- `npm run build` ผ่าน
- รัน `npm run lint` หากโปรเจกต์มีคำสั่งนี้ และแยกแจ้งปัญหาเดิมออกจากปัญหาที่เกิดจากการย้าย
- ไม่มี TypeScript error ที่เกิดจากการเปลี่ยนแปลง
- ทดสอบหน้าจอ desktop และ mobile
- mouse click, hover, Tab, Enter/Space และลูกศรซ้าย/ขวาทำงานตามเดิม
- ไม่มีการเปลี่ยน request/response ของ API, โครงสร้างข้อมูล, validation หรือ workflow ทางคลินิก
- สรุปไฟล์ที่เปลี่ยน วิธีทดสอบ และข้อจำกัดที่ยังเหลือให้ผู้ตรวจงานทราบ
