# Server Configuration
PORT=8080

# Database Configuration (PostgreSQL / Supabase Cloud DB)
# หากใช้ Supabase ให้ใส่ Host จาก Supabase Project Settings -> Database -> Connection string (เช่น aws-0-ap-southeast-1.pooler.supabase.com)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
DB_PORT=5432
DB_SSLMODE=disable

# JWT Secret Key
JWT_SECRET=supersecretclinicjwtkey2026
