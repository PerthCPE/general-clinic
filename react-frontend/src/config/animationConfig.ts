/**
 * =========================================================================
 * CLINIC ANIMATION & PERFORMANCE CONFIGURATION (>60 FPS / 120 FPS READY)
 * =========================================================================
 * สามารถปรับเปลี่ยนความเร็ว ความลื่นไหล และระยะเวลาการแสดงผลได้จากไฟล์นี้โดยตรง
 */

export const CLINIC_ANIMATION_CONFIG = {
  // -----------------------------------------------------------------------
  // 1. Frame Rate & Hardware Acceleration Target
  // -----------------------------------------------------------------------
  /** เป้าหมายความลื่นไหลของหน้าจอ (60Hz, 90Hz, 120Hz ProMotion, 144Hz Gaming) */
  targetFps: 120,
  /** เปิดใช้งาน GPU Hardware Acceleration (translate3d, will-change, backface-visibility) */
  gpuAccelerated: true,

  // -----------------------------------------------------------------------
  // 2. Display Duration Settings (มิลลิวินาที)
  // -----------------------------------------------------------------------
  /** ระยะเวลาแสดงผล Skeleton ขั้นต่ำ (ป้องกันอาการภาพกระพริบเมื่อ Cache ตอบสนองเร็วมาก) */
  minSkeletonLoadingMs: 380,

  /** ระยะเวลาแสดงผล Modal บันทึกข้อมูลลงฐานข้อมูล */
  submitModalDurationMs: 700,

  /** ระยะเวลาแสดง Toast การแจ้งเตือน */
  toastDurationMs: 3000,

  // -----------------------------------------------------------------------
  // 3. CSS Animation Speeds (วินาที)
  // -----------------------------------------------------------------------
  /** ความเร็วแสงเงาวิ่ง Shimmer ของ Skeleton */
  shimmerDurationSec: 1.25,

  /** ความเร็วหมุนของ Spinner */
  spinDurationSec: 0.85,

  /** ความเร็วการเด้งเปิดของ Modal (Scale-in) */
  modalScaleDurationSec: 0.22,

  /** ความเร็ว Fade In / Out */
  fadeDurationSec: 0.2,

  // -----------------------------------------------------------------------
  // 4. Easing Curves (ความโค้งของการเคลื่อนไหว)
  // -----------------------------------------------------------------------
  /** Spring Easing สไตล์ iOS / macOS ที่ให้ความรู้สึกนุ่มนวลและเป็นธรรมชาติ */
  springEasing: 'cubic-bezier(0.16, 1, 0.3, 1)',

  /** Standard Smooth Easing */
  smoothEasing: 'cubic-bezier(0.25, 1, 0.5, 1)',

  // -----------------------------------------------------------------------
  // 5. Scrolling Settings (การเลื่อนหน้าจอ)
  // -----------------------------------------------------------------------
  /** พฤติกรรมการเลื่อนหน้าจอของระบบคลังยาและการเงิน */
  scrollBehavior: 'smooth' as const,
};
